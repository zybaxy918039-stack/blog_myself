{
  "name": "blog-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "个人博客静态站点与 Cloudflare Pages 部署工具",
  "scripts": {
    "build": "node scripts/build.mjs",
    "prepare:cloudflare": "node scripts/prepare-cloudflare.mjs",
    "deploy": "node scripts/prepare-cloudflare.mjs && node scripts/build.mjs && wrangler pages deploy --branch main --config wrangler.ci.toml"
  },
  "devDependencies": {
    "cheerio": "^1.1.0",
    "gray-matter": "^4.0.3",
    "marked": "^15.0.0",
    "wrangler": "^4.0.0"
  }
}
