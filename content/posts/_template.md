---
title: 文章标题
category: essays
date: 2026-09-05
summary: 这里的文字会显示在分类页的文章摘要里。
tags:
  - 随笔
draft: false
---

在这里用 Markdown 写作。正文会由 GitHub Actions 构建成文章页。

分类可以填 `essays`（随笔）、`fiction`（小说）或 `tech`（技术）。
不想让一篇文章上线时，把 frontmatter 里的 `draft` 改为 `true` 即可。
